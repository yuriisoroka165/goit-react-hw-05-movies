// import MoviesDetails from "./MovieDetails";
import { MovieList } from "components/MoviesList/MoviesList";

function Home() {
    return (
        <main>
            <MovieList />
        </main>
    );
};

export default Home;
